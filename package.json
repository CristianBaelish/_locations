{
  "name": "live-street-pov",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "workspaces": ["server", "client"],
  "scripts": {
    "start": "npm run start -w server",
    "test": "npm run test -w server",
    "build": "npm run build -w client",
    "ship": "node scripts/push-main.mjs",
    "verify:deploy": "node scripts/verify-deployment.mjs",
    "verify:deploy:probe": "node scripts/verify-deployment.mjs --probe",
    "probe:timing": "node scripts/probe-timing.mjs",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:lan": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev:lan -w client\"",
    "dev:lan-https": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev:lan-https -w client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
