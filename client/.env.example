# Local: copia a `client/.env` o `.env` en la raíz del repo.
# Vercel: Project → Settings → Environment Variables (marcar Production).
#
# Google Cloud: Maps JavaScript API
VITE_GOOGLE_MAPS_API_KEY=

# Cuando el front esté en Vercel y el API en Render/Railway, sin barra final:
# VITE_API_ORIGIN=https://tu-servicio.onrender.com
